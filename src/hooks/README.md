This folder contains the functionality for the `--loader` and
`--import` arguments passed to Node.

Nothing in this section is allowed to `import 'typescript'`! All
TS actions must be done through the service client.
